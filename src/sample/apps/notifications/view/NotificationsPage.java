package sample.apps.notifications.view;

import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.layout.AnchorPane;
import sample.view.Page;

import java.io.IOException;

public class NotificationsPage extends Page {
    public NotificationsPage(){
        try {
            FXMLLoader fxmlLoader = new FXMLLoader(getClass().getResource("NotificationsPage.fxml"));
            Parent root = fxmlLoader.load();
            this.pane = new AnchorPane(root);
            this.controller = fxmlLoader.getController();
        }
        catch (IOException e) {
            e.printStackTrace();
        }
    }
}
