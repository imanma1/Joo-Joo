package sample.apps.notifications.view.request;

import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.layout.AnchorPane;
import sample.view.Page;

import java.io.IOException;

public class RequestsPage extends Page {
    public RequestsPage(){
        try {
            FXMLLoader fxmlLoader = new FXMLLoader(getClass().getResource("RequestsPage.fxml"));
            Parent root = fxmlLoader.load();
            this.pane = new AnchorPane(root);
            this.controller = fxmlLoader.getController();
        }
        catch (IOException e) {
            e.printStackTrace();
        }
    }
}
