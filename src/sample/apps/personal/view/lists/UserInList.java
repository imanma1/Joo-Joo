package sample.apps.personal.view.lists;

import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.layout.AnchorPane;
import sample.view.Page;

import java.io.IOException;

public class UserInList extends Page {
    public UserInList(){
        try {
            FXMLLoader fxmlLoader = new FXMLLoader(getClass().getResource("UserInList.fxml"));
            Parent root = fxmlLoader.load();
            this.pane = new AnchorPane(root);
            this.controller = fxmlLoader.getController();
        }
        catch (IOException e) {
            e.printStackTrace();
        }
    }
}
